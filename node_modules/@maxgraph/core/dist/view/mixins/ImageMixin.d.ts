import type { Graph } from '../Graph';
type PartialImage = Pick<Graph, 'imageBundles' | 'addImageBundle' | 'removeImageBundle' | 'getImageFromBundles'>;
type PartialType = PartialImage;
export declare const ImageMixin: PartialType;
export {};
